[package]
name = "said-shielded-pool-testvectors"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic, reproducible test vectors for the Ghola shielded pool. Source of truth for auditors, prover, on-chain program, and client implementations."

[[bin]]
name = "gen-vectors"
path = "src/main.rs"

[dependencies]
said-shielded-pool-types = { path = "../said-shielded-pool-types" }
serde = { workspace = true }
serde_json = { workspace = true }
hex = { workspace = true }
thiserror = { workspace = true }
rand = { workspace = true }
light-poseidon = "0.2"
ark-bn254 = "0.4"
ark-ff = "0.4"
