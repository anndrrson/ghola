Ghola 0.2.0 — Privacy update

End-to-end encrypted AI chat. Even we can't read your messages.

  • Pair Device flow — QR + ECDH handshake for cross-device sessions
  • Per-session vault in IndexedDB; encryption keys never leave your device
  • USDT support alongside USDC; pay per query in stablecoins
  • Server-blind architecture: chat content never reaches our servers in readable form
  • Production-hardened settlement, escrow, and on-chain accounting
  • Refreshed UI — privacy-led across browse, chat, and account screens

On Solana Seeker:
  • Hardware-backed identity via Seed Vault (when present)
  • Mobile Wallet Adapter for fast in-app payments
