diff --git a/dist/components/auth/OTP.js b/dist/components/auth/OTP.js
index 6ac2a9c22d3ba290b9f1a2650e82f455af470910..f92641684bc1302daffd9238c229fe513b99e036 100644
--- a/dist/components/auth/OTP.js
+++ b/dist/components/auth/OTP.js
@@ -13,6 +13,49 @@ var freeSolidSvgIcons = require('@fortawesome/free-solid-svg-icons');
 var clsx = require('clsx');
 var TurnstileWidget = require('./TurnstileWidget.js');
 
+function normalizeOtpCode(code) {
+  return String(code || "").trim().toUpperCase();
+}
+function otpErrorDiagnostic(error) {
+  const codes = [];
+  const statuses = [];
+  const activityIds = [];
+  let current = error;
+  for (let depth = 0; depth < 5 && current; depth += 1) {
+    if (typeof current !== "object") break;
+    if (typeof current.code === "string") codes.push(current.code);
+    if (typeof current.status === "number") statuses.push(current.status);
+    for (const key of ["activityId", "activity_id", "requestId", "request_id"]) {
+      if (typeof current[key] === "string") activityIds.push(current[key].slice(0, 128));
+    }
+    current = current.cause;
+  }
+  return {
+    phase: "completeOtp",
+    codes: [...new Set(codes)],
+    statuses: [...new Set(statuses)],
+    activityIds: [...new Set(activityIds)]
+  };
+}
+function friendlyOtpError(error) {
+  const { codes } = otpErrorDiagnostic(error);
+  if (codes.includes(core.TurnkeyErrorCodes.INVALID_OTP_CODE)) {
+    return "That code is invalid or expired. Start over and use the newest code.";
+  }
+  if (codes.includes(core.TurnkeyErrorCodes.MAX_OTP_INITIATED_ERROR)) {
+    return "Too many verification attempts. Start over after the cooldown.";
+  }
+  if (codes.some(code => [
+    core.TurnkeyErrorCodes.NETWORK_ERROR,
+    core.TurnkeyErrorCodes.TIMEOUT,
+    core.TurnkeyErrorCodes.SERVICE_UNAVAILABLE,
+    core.TurnkeyErrorCodes.GATEWAY_TIMEOUT
+  ].includes(code))) {
+    return "Turnkey could not verify the code. Start over when the connection recovers.";
+  }
+  return "The code was accepted or rejected, but secure sign-in did not finish. Start over; do not retry it.";
+}
+
 function OtpVerification(props) {
   const {
     contact,
@@ -21,6 +64,7 @@ function OtpVerification(props) {
     alphanumeric = true,
     formattedContact,
     sessionKey,
+    publicKey,
     onContinue = null // Default to null if not provided
   } = props;
   const {
@@ -38,6 +82,8 @@ function OtpVerification(props) {
   const [otpEncryptionTargetBundle, setOtpEncryptionTargetBundle] = react.useState(props.otpEncryptionTargetBundle);
   const [error, setError] = react.useState(null);
   const [shaking, setShaking] = react.useState(false);
+  const [verificationAttempted, setVerificationAttempted] = react.useState(false);
+  const verificationAttemptedRef = react.useRef(false);
   // Deferred: entering the code doesn't need a captcha, only resending does. The
   // widget stays unmounted until the user actually asks for a new code.
   const {
@@ -55,17 +101,23 @@ function OtpVerification(props) {
     setTimeout(() => setShaking(false), 250);
   };
   const handleContinue = async otpCode => {
+    if (verificationAttemptedRef.current) return;
+    const normalizedOtpCode = normalizeOtpCode(otpCode);
+    verificationAttemptedRef.current = true;
+    setVerificationAttempted(true);
+    setError(null);
     try {
       setSubmitting(true);
       if (onContinue) {
-        await onContinue(otpCode);
+        await onContinue(normalizedOtpCode);
       } else {
         await completeOtp({
           otpId,
-          otpCode,
+          otpCode: normalizedOtpCode,
           otpEncryptionTargetBundle,
           contact,
           otpType,
+          publicKey,
           ...(sessionKey && {
             sessionKey
           })
@@ -73,10 +125,9 @@ function OtpVerification(props) {
         closeModal();
       }
     } catch (error) {
-      const niceError = error.code === core.TurnkeyErrorCodes.INVALID_OTP_CODE ? "Invalid OTP code" : "An error has occurred";
-      setError(niceError);
+      console.error("[Turnkey OTP] verification rejected", JSON.stringify(otpErrorDiagnostic(error)));
+      setError(friendlyOtpError(error));
       shakeInput();
-      throw new Error(`Error completing OTP: ${error}`);
     } finally {
       setSubmitting(false);
     }
@@ -100,9 +151,15 @@ function OtpVerification(props) {
       });
       setOtpId(otpId);
       setOtpEncryptionTargetBundle(otpEncryptionTargetBundle);
+      verificationAttemptedRef.current = false;
+      setVerificationAttempted(false);
       setResent(true);
     } catch (error) {
-      throw new Error(`Error resending OTP: ${error}`);
+      console.error("[Turnkey OTP] resend failed", JSON.stringify({
+        ...otpErrorDiagnostic(error),
+        phase: "initOtp"
+      }));
+      setError("Could not send a new code. Start over and try again later.");
     } finally {
       setResending(false);
     }
@@ -128,8 +185,9 @@ function OtpVerification(props) {
         children: jsxRuntime.jsx(OtpInput, {
           otpLength: otpLength,
           onContinue: handleContinue,
-          alphanumeric: alphanumeric
-        })
+          alphanumeric: alphanumeric,
+          disabled: submitting || verificationAttempted
+        }, otpId)
       }), error && jsxRuntime.jsx("div", {
         className: "text-red-400 text-center text-sm",
         children: error
@@ -157,7 +215,8 @@ function OtpInput(props) {
   const {
     otpLength,
     onContinue,
-    alphanumeric = false
+    alphanumeric = false,
+    disabled = false
   } = props;
   const {
     isMobile
@@ -165,6 +224,7 @@ function OtpInput(props) {
   const [values, setValues] = react.useState(Array(otpLength).fill(""));
   const inputsRef = react.useRef([]);
   const handleChange = (index, value) => {
+    if (disabled) return;
     if (!alphanumeric) value = value.replace(/[^0-9]/g, "");else value = value.replace(/[^a-zA-Z0-9]/g, "");
     const newValues = [...values];
     newValues[index] = value.slice(-1); // only last char
@@ -177,6 +237,7 @@ function OtpInput(props) {
     }
   };
   const handleKeyDown = (e, index) => {
+    if (disabled) return;
     if (e.key === "Backspace" && !values[index] && index > 0) {
       inputsRef.current[index - 1]?.focus();
     }
@@ -186,12 +247,13 @@ function OtpInput(props) {
     if (e.key === "ArrowRight" && index < otpLength - 1) {
       inputsRef.current[index + 1]?.focus();
     }
-    if (e.key === "Enter") {
+    if (e.key === "Enter" && values.every(value => value)) {
       onContinue(values.join(""));
     }
   };
   const handlePaste = e => {
     e.preventDefault();
+    if (disabled) return;
     const paste = e.clipboardData.getData("text").trim();
     const cleaned = alphanumeric ? paste.replace(/[^a-zA-Z0-9]/g, "") : paste.replace(/[^0-9]/g, "");
     const sliced = cleaned.slice(0, otpLength).split("");
@@ -206,7 +268,7 @@ function OtpInput(props) {
     // Focus the last filled box
     const lastIndex = Math.min(sliced.length, otpLength - 1);
     inputsRef.current[lastIndex]?.focus();
-    onContinue(newValues.join(""));
+    if (newValues.every(value => value)) onContinue(newValues.join(""));
   };
   return jsxRuntime.jsx("div", {
     className: clsx("flex items-center justify-center space-x-2", isMobile ? "w-[95%]" : "w-full"),
@@ -219,6 +281,7 @@ function OtpInput(props) {
         maxLength: 1,
         value: values[i]?.toUpperCase(),
         autoComplete: "off",
+        disabled: disabled,
         onChange: e => handleChange(i, e.target.value.toUpperCase()),
         onKeyDown: e => handleKeyDown(e, i),
         onPaste: handlePaste,
@@ -231,4 +294,5 @@ function OtpInput(props) {
 
 exports.OtpInput = OtpInput;
 exports.OtpVerification = OtpVerification;
+exports.normalizeOtpCode = normalizeOtpCode;
 //# sourceMappingURL=OTP.js.map
diff --git a/dist/components/auth/OTP.mjs b/dist/components/auth/OTP.mjs
index 6b42b8e645d2c42aba72a6680e4c76c35e8fc912..6c0f6509b2af261131624861a25a154768dbe17a 100644
--- a/dist/components/auth/OTP.mjs
+++ b/dist/components/auth/OTP.mjs
@@ -12,6 +12,46 @@ import { faEnvelope, faPhone } from '@fortawesome/free-solid-svg-icons';
 import clsx from 'clsx';
 import { useTurnstile } from './TurnstileWidget.mjs';
 
+function normalizeOtpCode(code) {
+  return String(code || "").trim().toUpperCase();
+}
+function otpErrorDiagnostic(error) {
+  var codes = [];
+  var statuses = [];
+  var activityIds = [];
+  var current = error;
+  for (var depth = 0; depth < 5 && current; depth += 1) {
+    if (typeof current !== "object") break;
+    if (typeof current.code === "string") codes.push(current.code);
+    if (typeof current.status === "number") statuses.push(current.status);
+    ["activityId", "activity_id", "requestId", "request_id"].forEach(function (key) {
+      if (typeof current[key] === "string") activityIds.push(current[key].slice(0, 128));
+    });
+    current = current.cause;
+  }
+  return {
+    phase: "completeOtp",
+    codes: _toConsumableArray(new Set(codes)),
+    statuses: _toConsumableArray(new Set(statuses)),
+    activityIds: _toConsumableArray(new Set(activityIds))
+  };
+}
+function friendlyOtpError(error) {
+  var codes = otpErrorDiagnostic(error).codes;
+  if (codes.includes(TurnkeyErrorCodes.INVALID_OTP_CODE)) {
+    return "That code is invalid or expired. Start over and use the newest code.";
+  }
+  if (codes.includes(TurnkeyErrorCodes.MAX_OTP_INITIATED_ERROR)) {
+    return "Too many verification attempts. Start over after the cooldown.";
+  }
+  if (codes.some(function (code) {
+    return [TurnkeyErrorCodes.NETWORK_ERROR, TurnkeyErrorCodes.TIMEOUT, TurnkeyErrorCodes.SERVICE_UNAVAILABLE, TurnkeyErrorCodes.GATEWAY_TIMEOUT].includes(code);
+  })) {
+    return "Turnkey could not verify the code. Start over when the connection recovers.";
+  }
+  return "The code was accepted or rejected, but secure sign-in did not finish. Start over; do not retry it.";
+}
+
 function OtpVerification(props) {
   var contact = props.contact,
     otpType = props.otpType,
@@ -21,6 +61,7 @@ function OtpVerification(props) {
     alphanumeric = _props$alphanumeric === void 0 ? true : _props$alphanumeric,
     formattedContact = props.formattedContact,
     sessionKey = props.sessionKey,
+    publicKey = props.publicKey,
     _props$onContinue = props.onContinue,
     onContinue = _props$onContinue === void 0 ? null : _props$onContinue;
   var _useTurnkey = useTurnkey(),
@@ -57,6 +98,11 @@ function OtpVerification(props) {
     _useState12 = _slicedToArray(_useState11, 2),
     shaking = _useState12[0],
     setShaking = _useState12[1];
+  var _useState13 = useState(false),
+    _useState14 = _slicedToArray(_useState13, 2),
+    verificationAttempted = _useState14[0],
+    setVerificationAttempted = _useState14[1];
+  var verificationAttemptedRef = useRef(false);
   // Deferred: entering the code doesn't need a captcha, only resending does. The
   // widget stays unmounted until the user actually asks for a new code.
   var _useTurnstile = useTurnstile({
@@ -76,10 +122,15 @@ function OtpVerification(props) {
   };
   var handleContinue = /*#__PURE__*/function () {
     var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(otpCode) {
-      var niceError, _t;
+      var normalizedOtpCode, _t;
       return _regenerator().w(function (_context) {
         while (1) switch (_context.p = _context.n) {
           case 0:
+            if (verificationAttemptedRef.current) return _context.a(2);
+            normalizedOtpCode = normalizeOtpCode(otpCode);
+            verificationAttemptedRef.current = true;
+            setVerificationAttempted(true);
+            setError(null);
             _context.p = 0;
             setSubmitting(true);
             if (!onContinue) {
@@ -87,7 +138,7 @@ function OtpVerification(props) {
               break;
             }
             _context.n = 1;
-            return onContinue(otpCode);
+            return onContinue(normalizedOtpCode);
           case 1:
             _context.n = 4;
             break;
@@ -95,10 +146,11 @@ function OtpVerification(props) {
             _context.n = 3;
             return completeOtp(_objectSpread2({
               otpId: otpId,
-              otpCode: otpCode,
+              otpCode: normalizedOtpCode,
               otpEncryptionTargetBundle: otpEncryptionTargetBundle,
               contact: contact,
-              otpType: otpType
+              otpType: otpType,
+              publicKey: publicKey
             }, sessionKey && {
               sessionKey: sessionKey
             }));
@@ -110,10 +162,11 @@ function OtpVerification(props) {
           case 5:
             _context.p = 5;
             _t = _context.v;
-            niceError = _t.code === TurnkeyErrorCodes.INVALID_OTP_CODE ? "Invalid OTP code" : "An error has occurred";
-            setError(niceError);
+            console.error("[Turnkey OTP] verification rejected", JSON.stringify(otpErrorDiagnostic(_t)));
+            setError(friendlyOtpError(_t));
             shakeInput();
-            throw new Error("Error completing OTP: ".concat(_t));
+            _context.n = 6;
+            break;
           case 6:
             _context.p = 6;
             setSubmitting(false);
@@ -158,13 +211,20 @@ function OtpVerification(props) {
             _otpEncryptionTargetBundle = _yield$initOtp.otpEncryptionTargetBundle;
             setOtpId(_otpId);
             setOtpEncryptionTargetBundle(_otpEncryptionTargetBundle);
+            verificationAttemptedRef.current = false;
+            setVerificationAttempted(false);
             setResent(true);
             _context2.n = 6;
             break;
           case 5:
             _context2.p = 5;
             _t2 = _context2.v;
-            throw new Error("Error resending OTP: ".concat(_t2));
+            console.error("[Turnkey OTP] resend failed", JSON.stringify(_objectSpread2(_objectSpread2({}, otpErrorDiagnostic(_t2)), {}, {
+              phase: "initOtp"
+            })));
+            setError("Could not send a new code. Start over and try again later.");
+            _context2.n = 6;
+            break;
           case 6:
             _context2.p = 6;
             setResending(false);
@@ -199,8 +259,9 @@ function OtpVerification(props) {
         children: jsx(OtpInput, {
           otpLength: otpLength,
           onContinue: handleContinue,
-          alphanumeric: alphanumeric
-        })
+          alphanumeric: alphanumeric,
+          disabled: submitting || verificationAttempted
+        }, otpId)
       }), error && jsx("div", {
         className: "text-red-400 text-center text-sm",
         children: error
@@ -228,15 +289,18 @@ function OtpInput(props) {
   var otpLength = props.otpLength,
     onContinue = props.onContinue,
     _props$alphanumeric2 = props.alphanumeric,
-    alphanumeric = _props$alphanumeric2 === void 0 ? false : _props$alphanumeric2;
+    alphanumeric = _props$alphanumeric2 === void 0 ? false : _props$alphanumeric2,
+    _props$disabled = props.disabled,
+    disabled = _props$disabled === void 0 ? false : _props$disabled;
   var _useModal2 = useModal(),
     isMobile = _useModal2.isMobile;
-  var _useState13 = useState(Array(otpLength).fill("")),
-    _useState14 = _slicedToArray(_useState13, 2),
-    values = _useState14[0],
-    setValues = _useState14[1];
+  var _useState15 = useState(Array(otpLength).fill("")),
+    _useState16 = _slicedToArray(_useState15, 2),
+    values = _useState16[0],
+    setValues = _useState16[1];
   var inputsRef = useRef([]);
   var handleChange = function handleChange(index, value) {
+    if (disabled) return;
     if (!alphanumeric) value = value.replace(/[^0-9]/g, "");else value = value.replace(/[^a-zA-Z0-9]/g, "");
     var newValues = _toConsumableArray(values);
     newValues[index] = value.slice(-1); // only last char
@@ -252,6 +316,7 @@ function OtpInput(props) {
     }
   };
   var handleKeyDown = function handleKeyDown(e, index) {
+    if (disabled) return;
     if (e.key === "Backspace" && !values[index] && index > 0) {
       var _inputsRef$current2;
       (_inputsRef$current2 = inputsRef.current[index - 1]) === null || _inputsRef$current2 === void 0 ? void 0 : _inputsRef$current2.focus();
@@ -264,13 +329,16 @@ function OtpInput(props) {
       var _inputsRef$current4;
       (_inputsRef$current4 = inputsRef.current[index + 1]) === null || _inputsRef$current4 === void 0 ? void 0 : _inputsRef$current4.focus();
     }
-    if (e.key === "Enter") {
+    if (e.key === "Enter" && values.every(function (value) {
+      return value;
+    })) {
       onContinue(values.join(""));
     }
   };
   var handlePaste = function handlePaste(e) {
     var _inputsRef$current$la;
     e.preventDefault();
+    if (disabled) return;
     var paste = e.clipboardData.getData("text").trim();
     var cleaned = alphanumeric ? paste.replace(/[^a-zA-Z0-9]/g, "") : paste.replace(/[^0-9]/g, "");
     var sliced = cleaned.slice(0, otpLength).split("");
@@ -287,7 +355,9 @@ function OtpInput(props) {
     // Focus the last filled box
     var lastIndex = Math.min(sliced.length, otpLength - 1);
     (_inputsRef$current$la = inputsRef.current[lastIndex]) === null || _inputsRef$current$la === void 0 ? void 0 : _inputsRef$current$la.focus();
-    onContinue(newValues.join(""));
+    if (newValues.every(function (value) {
+      return value;
+    })) onContinue(newValues.join(""));
   };
   return jsx("div", {
     className: clsx("flex items-center justify-center space-x-2", isMobile ? "w-[95%]" : "w-full"),
@@ -301,6 +371,7 @@ function OtpInput(props) {
         maxLength: 1,
         value: (_values$i = values[i]) === null || _values$i === void 0 ? void 0 : _values$i.toUpperCase(),
         autoComplete: "off",
+        disabled: disabled,
         onChange: function onChange(e) {
           return handleChange(i, e.target.value.toUpperCase());
         },
@@ -317,5 +388,5 @@ function OtpInput(props) {
   });
 }
 
-export { OtpInput, OtpVerification };
+export { OtpInput, OtpVerification, normalizeOtpCode };
 //# sourceMappingURL=OTP.mjs.map
diff --git a/dist/components/auth/TurnstileWidget.js b/dist/components/auth/TurnstileWidget.js
index fc3cfde9f82a527b39ad59026e66eb4f9bd477ff..af34bda1876dad8de9c83a44d29407ded64d113b 100644
--- a/dist/components/auth/TurnstileWidget.js
+++ b/dist/components/auth/TurnstileWidget.js
@@ -54,6 +54,7 @@ function useTurnstile(options) {
     pendingToken.current = null;
   };
   const consumeToken = async () => {
+    if (!turnstileConfigured) return {};
     const result = await captcha.consumeCaptchaToken(getTurnstileToken, token => {
       setTurnstileToken(token);
       // Cleared on consume/reset — keep actions disabled until onSuccess.
diff --git a/dist/components/auth/TurnstileWidget.mjs b/dist/components/auth/TurnstileWidget.mjs
index 0f4c161f58bc38e0c1a0f6bca4f11f66229d86bd..5fc3b555f9cf68e726fdfca4a4a092e1d75fe4e0 100644
--- a/dist/components/auth/TurnstileWidget.mjs
+++ b/dist/components/auth/TurnstileWidget.mjs
@@ -82,7 +82,13 @@ function useTurnstile(options) {
       return _regenerator().w(function (_context) {
         while (1) switch (_context.n) {
           case 0:
-            _context.n = 1;
+            if (turnstileConfigured) {
+              _context.n = 1;
+              break;
+            }
+            return _context.a(2, {});
+          case 1:
+            _context.n = 2;
             return consumeCaptchaToken(getTurnstileToken, function (token) {
               setTurnstileToken(token);
               // Cleared on consume/reset — keep actions disabled until onSuccess.
@@ -90,7 +96,7 @@ function useTurnstile(options) {
                 setAuthEnabled(false);
               }
             }, turnstileRef);
-          case 1:
+          case 2:
             result = _context.v;
             // Timed out with no token — ensure gated actions stay disabled.
             if (turnstileConfigured && !("captchaToken" in result)) {
diff --git a/dist/components/auth/index.js b/dist/components/auth/index.js
index 5ee5880fabef3171a3790eaa483ea3834c157962..18ed29ed0985bb9c0bda0dbcbdaa7c4ac705c5d3 100644
--- a/dist/components/auth/index.js
+++ b/dist/components/auth/index.js
@@ -1,6 +1,7 @@
 'use strict';
 
 var jsxRuntime = require('react/jsx-runtime');
+var react = require('react');
 var reactFontawesome = require('@fortawesome/react-fontawesome');
 var freeBrandsSvgIcons = require('@fortawesome/free-brands-svg-icons');
 var core = require('@turnkey/core');
@@ -37,6 +38,8 @@ function AuthComponent({
     handleXOauth,
     handleDiscordOauth,
     initOtp,
+    createApiKeyPair,
+    deleteApiKeyPair,
     loginWithPasskey,
     signUpWithPasskey
   } = Hook.useTurnkey();
@@ -50,6 +53,16 @@ function AuthComponent({
     consumeToken,
     authEnabled
   } = TurnstileWidget.useTurnstile();
+  const otpInitializationRef = react.useRef(null);
+  const beginOtpInitialization = operation => {
+    if (otpInitializationRef.current) return otpInitializationRef.current;
+    const attempt = operation();
+    otpInitializationRef.current = attempt;
+    void attempt.catch(() => {
+      if (otpInitializationRef.current === attempt) otpInitializationRef.current = null;
+    });
+    return attempt;
+  };
   if (!config || clientState === base.ClientState.Loading) {
     // Don't check ClientState.Error here. We already check in the modal root
     return jsxRuntime.jsx("div", {
@@ -65,8 +78,10 @@ function AuthComponent({
     methodOrder = [],
     oauthOrder = []
   } = config.ui?.authModal || {};
-  const handleEmailSubmit = async email => {
+  const handleEmailSubmit = email => beginOtpInitialization(async () => {
+    let publicKey;
     try {
+      publicKey = await createApiKeyPair();
       const {
         otpId,
         otpEncryptionTargetBundle
@@ -81,6 +96,7 @@ function AuthComponent({
           contact: email,
           otpId: otpId,
           otpEncryptionTargetBundle: otpEncryptionTargetBundle,
+          publicKey: publicKey,
           otpType: core.OtpType.Email,
           otpLength: config.auth?.otpLength !== undefined ? Number(config.auth.otpLength) : undefined,
           alphanumeric: config.auth?.otpAlphanumeric,
@@ -91,11 +107,16 @@ function AuthComponent({
         showTitle: false
       });
     } catch (error) {
+      if (publicKey) await deleteApiKeyPair({
+        publicKey
+      }).catch(() => undefined);
       throw new Error(`Error initializing OTP: ${error}`);
     }
-  };
-  const handlePhoneSubmit = async (phone, formattedPhone) => {
+  });
+  const handlePhoneSubmit = (phone, formattedPhone) => beginOtpInitialization(async () => {
+    let publicKey;
     try {
+      publicKey = await createApiKeyPair();
       const {
         otpId,
         otpEncryptionTargetBundle
@@ -112,6 +133,7 @@ function AuthComponent({
           formattedContact: formattedPhone,
           otpId: otpId,
           otpEncryptionTargetBundle: otpEncryptionTargetBundle,
+          publicKey: publicKey,
           otpType: core.OtpType.Sms,
           otpLength: config.auth?.otpLength !== undefined ? Number(config.auth.otpLength) : undefined,
           alphanumeric: config.auth?.otpAlphanumeric,
@@ -122,9 +144,12 @@ function AuthComponent({
         showTitle: false
       });
     } catch (error) {
+      if (publicKey) await deleteApiKeyPair({
+        publicKey
+      }).catch(() => undefined);
       throw new Error(`Error initializing OTP: ${error}`);
     }
-  };
+  });
   const handlePasskeyLogin = () => {
     pushPage({
       key: "Passkey Login",
diff --git a/dist/components/auth/index.mjs b/dist/components/auth/index.mjs
index 3671f707007f3779aa7da942aafbe4e1eb818a66..3dbc5b113e50695f1721b5b7f98f28e70bd8e89e 100644
--- a/dist/components/auth/index.mjs
+++ b/dist/components/auth/index.mjs
@@ -1,5 +1,6 @@
 import { asyncToGenerator as _asyncToGenerator, regenerator as _regenerator, objectSpread2 as _objectSpread2 } from '../../_virtual/_rollupPluginBabelHelpers.mjs';
 import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
+import { useRef } from 'react';
 import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
 import { faDiscord, faXTwitter, faFacebook, faApple, faGoogle } from '@fortawesome/free-brands-svg-icons';
 import { OtpType } from '@turnkey/core';
@@ -36,6 +37,8 @@ function AuthComponent(_ref) {
     handleXOauth = _useTurnkey.handleXOauth,
     handleDiscordOauth = _useTurnkey.handleDiscordOauth,
     initOtp = _useTurnkey.initOtp,
+    createApiKeyPair = _useTurnkey.createApiKeyPair,
+    deleteApiKeyPair = _useTurnkey.deleteApiKeyPair,
     loginWithPasskey = _useTurnkey.loginWithPasskey,
     signUpWithPasskey = _useTurnkey.signUpWithPasskey;
   var _useModal = useModal(),
@@ -46,6 +49,16 @@ function AuthComponent(_ref) {
     turnstile = _useTurnstile.turnstile,
     consumeToken = _useTurnstile.consumeToken,
     authEnabled = _useTurnstile.authEnabled;
+  var otpInitializationRef = useRef(null);
+  var beginOtpInitialization = function beginOtpInitialization(operation) {
+    if (otpInitializationRef.current) return otpInitializationRef.current;
+    var attempt = operation();
+    otpInitializationRef.current = attempt;
+    void attempt.catch(function () {
+      if (otpInitializationRef.current === attempt) otpInitializationRef.current = null;
+    });
+    return attempt;
+  };
   if (!config || clientState === ClientState.Loading) {
     // Don't check ClientState.Error here. We already check in the modal root
     return jsx("div", {
@@ -63,110 +76,77 @@ function AuthComponent(_ref) {
     methodOrder = _ref2$methodOrder === void 0 ? [] : _ref2$methodOrder,
     _ref2$oauthOrder = _ref2.oauthOrder,
     oauthOrder = _ref2$oauthOrder === void 0 ? [] : _ref2$oauthOrder;
-  var handleEmailSubmit = /*#__PURE__*/function () {
-    var _ref3 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(email) {
-      var _config$auth, _config$auth2, _yield$initOtp, otpId, otpEncryptionTargetBundle, _t, _t2, _t3, _t4;
-      return _regenerator().w(function (_context) {
-        while (1) switch (_context.p = _context.n) {
-          case 0:
-            _context.p = 0;
-            _t = initOtp;
-            _t2 = _objectSpread2;
-            _t3 = {
-              otpType: OtpType.Email,
-              contact: email
-            };
-            _context.n = 1;
-            return consumeToken();
-          case 1:
-            _context.n = 2;
-            return _t(_t2(_t3, _context.v));
-          case 2:
-            _yield$initOtp = _context.v;
-            otpId = _yield$initOtp.otpId;
-            otpEncryptionTargetBundle = _yield$initOtp.otpEncryptionTargetBundle;
-            pushPage({
-              key: "Verify OTP",
-              content: jsx(OtpVerification, _objectSpread2({
-                contact: email,
-                otpId: otpId,
-                otpEncryptionTargetBundle: otpEncryptionTargetBundle,
-                otpType: OtpType.Email,
-                otpLength: ((_config$auth = config.auth) === null || _config$auth === void 0 ? void 0 : _config$auth.otpLength) !== undefined ? Number(config.auth.otpLength) : undefined,
-                alphanumeric: (_config$auth2 = config.auth) === null || _config$auth2 === void 0 ? void 0 : _config$auth2.otpAlphanumeric
-              }, sessionKey && {
-                sessionKey: sessionKey
-              })),
-              showTitle: false
-            });
-            _context.n = 4;
-            break;
-          case 3:
-            _context.p = 3;
-            _t4 = _context.v;
-            throw new Error("Error initializing OTP: ".concat(_t4));
-          case 4:
-            return _context.a(2);
-        }
-      }, _callee, null, [[0, 3]]);
-    }));
-    return function handleEmailSubmit(_x) {
-      return _ref3.apply(this, arguments);
-    };
-  }();
-  var handlePhoneSubmit = /*#__PURE__*/function () {
-    var _ref4 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2(phone, formattedPhone) {
-      var _config$auth3, _config$auth4, _yield$initOtp2, otpId, otpEncryptionTargetBundle, _t5, _t6, _t7, _t8;
-      return _regenerator().w(function (_context2) {
-        while (1) switch (_context2.p = _context2.n) {
-          case 0:
-            _context2.p = 0;
-            _t5 = initOtp;
-            _t6 = _objectSpread2;
-            _t7 = {
-              otpType: OtpType.Sms,
-              contact: phone
-            };
-            _context2.n = 1;
-            return consumeToken();
-          case 1:
-            _context2.n = 2;
-            return _t5(_t6(_t7, _context2.v));
-          case 2:
-            _yield$initOtp2 = _context2.v;
-            otpId = _yield$initOtp2.otpId;
-            otpEncryptionTargetBundle = _yield$initOtp2.otpEncryptionTargetBundle;
-            pushPage({
-              key: "Verify OTP",
-              content: jsx(OtpVerification, _objectSpread2({
-                contact: phone,
-                // Pass in the formatted phone number seperately. In the case that some weird formatting occurs, we don't want to send it into the initOtp request
-                formattedContact: formattedPhone,
-                otpId: otpId,
-                otpEncryptionTargetBundle: otpEncryptionTargetBundle,
-                otpType: OtpType.Sms,
-                otpLength: ((_config$auth3 = config.auth) === null || _config$auth3 === void 0 ? void 0 : _config$auth3.otpLength) !== undefined ? Number(config.auth.otpLength) : undefined,
-                alphanumeric: (_config$auth4 = config.auth) === null || _config$auth4 === void 0 ? void 0 : _config$auth4.otpAlphanumeric
-              }, sessionKey && {
-                sessionKey: sessionKey
-              })),
-              showTitle: false
-            });
-            _context2.n = 4;
-            break;
-          case 3:
-            _context2.p = 3;
-            _t8 = _context2.v;
-            throw new Error("Error initializing OTP: ".concat(_t8));
-          case 4:
-            return _context2.a(2);
-        }
-      }, _callee2, null, [[0, 3]]);
-    }));
-    return function handlePhoneSubmit(_x2, _x3) {
-      return _ref4.apply(this, arguments);
-    };
-  }();
+  var startEmailOtp = async function startEmailOtp(email) {
+    var publicKey;
+    try {
+      publicKey = await createApiKeyPair();
+      var _yield$initOtp = await initOtp(_objectSpread2({
+          otpType: OtpType.Email,
+          contact: email
+        }, await consumeToken())),
+        otpId = _yield$initOtp.otpId,
+        otpEncryptionTargetBundle = _yield$initOtp.otpEncryptionTargetBundle;
+      pushPage({
+        key: "Verify OTP",
+        content: jsx(OtpVerification, _objectSpread2({
+          contact: email,
+          otpId: otpId,
+          otpEncryptionTargetBundle: otpEncryptionTargetBundle,
+          publicKey: publicKey,
+          otpType: OtpType.Email,
+          otpLength: config.auth?.otpLength !== undefined ? Number(config.auth.otpLength) : undefined,
+          alphanumeric: config.auth?.otpAlphanumeric
+        }, sessionKey && {
+          sessionKey: sessionKey
+        })),
+        showTitle: false
+      });
+    } catch (error) {
+      if (publicKey) await deleteApiKeyPair({ publicKey: publicKey }).catch(function () {});
+      throw new Error("Error initializing OTP: ".concat(error));
+    }
+  };
+  var handleEmailSubmit = function handleEmailSubmit(email) {
+    return beginOtpInitialization(function () {
+      return startEmailOtp(email);
+    });
+  };
+  var startPhoneOtp = async function startPhoneOtp(phone, formattedPhone) {
+    var publicKey;
+    try {
+      publicKey = await createApiKeyPair();
+      var _yield$initOtp2 = await initOtp(_objectSpread2({
+          otpType: OtpType.Sms,
+          contact: phone
+        }, await consumeToken())),
+        otpId = _yield$initOtp2.otpId,
+        otpEncryptionTargetBundle = _yield$initOtp2.otpEncryptionTargetBundle;
+      pushPage({
+        key: "Verify OTP",
+        content: jsx(OtpVerification, _objectSpread2({
+          contact: phone,
+          formattedContact: formattedPhone,
+          otpId: otpId,
+          otpEncryptionTargetBundle: otpEncryptionTargetBundle,
+          publicKey: publicKey,
+          otpType: OtpType.Sms,
+          otpLength: config.auth?.otpLength !== undefined ? Number(config.auth.otpLength) : undefined,
+          alphanumeric: config.auth?.otpAlphanumeric
+        }, sessionKey && {
+          sessionKey: sessionKey
+        })),
+        showTitle: false
+      });
+    } catch (error) {
+      if (publicKey) await deleteApiKeyPair({ publicKey: publicKey }).catch(function () {});
+      throw new Error("Error initializing OTP: ".concat(error));
+    }
+  };
+  var handlePhoneSubmit = function handlePhoneSubmit(phone, formattedPhone) {
+    return beginOtpInitialization(function () {
+      return startPhoneOtp(phone, formattedPhone);
+    });
+  };
   var handlePasskeyLogin = function handlePasskeyLogin() {
     pushPage({
       key: "Passkey Login",
diff --git a/dist/providers/modal/Provider.js b/dist/providers/modal/Provider.js
index 56918d7f41618a8b4cd572364fa6d2aed5a3d804..3ffd7c2525c65e770dd58ba4c28040c8f5d1437c 100644
--- a/dist/providers/modal/Provider.js
+++ b/dist/providers/modal/Provider.js
@@ -23,6 +23,9 @@ function ModalProvider({
   const closeModal = () => {
     setModalStack([]);
     setSheet(null);
+    if (typeof window !== "undefined") {
+      window.dispatchEvent(new Event("ghola:turnkey-auth-modal-closed"));
+    }
   };
   const closeSheet = () => setSheet(null);
   return jsxRuntime.jsx(ModalContext.Provider, {
diff --git a/dist/providers/modal/Provider.mjs b/dist/providers/modal/Provider.mjs
index 00ba26c8d1d4e211530507362e576feb9c855631..b3c4a42f1882c3261a42c7efe230cdfb3806dbbc 100644
--- a/dist/providers/modal/Provider.mjs
+++ b/dist/providers/modal/Provider.mjs
@@ -42,6 +42,9 @@ function ModalProvider(_ref) {
   var closeModal = function closeModal() {
     setModalStack([]);
     setSheet(null);
+    if (typeof window !== "undefined") {
+      window.dispatchEvent(new Event("ghola:turnkey-auth-modal-closed"));
+    }
   };
   var closeSheet = function closeSheet() {
     return setSheet(null);
