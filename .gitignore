/target
**/target
.DS_Store
node_modules
.next
*.tsbuildinfo
android/.gradle/
android/build/
android/app/build/
# CMake build cache — was accidentally committed historically, now ignored
android/app/.cxx/
*.apk
*.idsig
# Release signing (never commit)
android/keystore/
*.keystore
*.jks
# Solana dApp Store publisher keypair and built APK
android/dapp-store/publisher.json
android/dapp-store/app-release.apk
# Firebase config (if added in a future phase)
android/app/google-services.json
.idea/
*.iml
local.properties
.claude/
.vercel
.dev/
ios/Ghola.xcodeproj/
ios/build/
.env*.local

# Terraform local plans, state, and operator-specific variable files.
*.tfstate
*.tfstate.*
*.tfplan
*.plan
*.auto.tfvars
*.auto.tfvars.json

# Stream 10 — hardening evidence raw artifacts (manifests live elsewhere)
/artifacts/hardening-evidence/

# Local native dev scratch build output (CMake)
mac-dev/build/

# ZK circuit / ceremony artifacts — regenerable or public; never commit.
# Powers-of-Tau (.ptau) are public; r1cs/sym/wtns are circom compiler outputs.
# These exceed/approach GitHub size limits and bloat history. Fetch/generate locally.
*.ptau
*.r1cs
*.sym
*.wtns
